spring:
  data:
    mongodb:
      host: ${MYSQL_DATABASE}
      port: 27017
      username: ${MYSQL_USER}
      password: ${MYSQL_ROOT_PASSWORD}
      database: users
      authentication-database: admin

server:
  port: 8080
